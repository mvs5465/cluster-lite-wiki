apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-lite-wiki.fullname" . }}
  labels:
    {{- include "cluster-lite-wiki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-lite-wiki.selectorLabels" . | nindent 4 }}
